@model IEnumerable<Models.Class> 
@{
    ViewBag.Title = "Class";
    Layout = "~/Views/Shared/_LayoutOrganization.cshtml";
}

@Html.Partial("_NavigationBar", (string)ViewBag.ID)

<section id="content">
    @Html.Partial("_ProfileBar")
    <main>
        <div class="d-flex justify-content-between">
            <div>
                <h1 class="title">Class</h1>
                <ul class="breadcrumbs">
                    <li><a href="#">Organization</a></li>
                    <li class="divider"></li>
                    <li><a href="#" class="active">Class</a></li>
                </ul>
            </div>
            <div>
                <a href="@Url.Action("AddClass","Organization",new {ID = (string)ViewBag.ID})"
                   class="btn text-white" style="background-color: #f96b36;min-width: 120px;">Create Class</a>
            </div>
        </div>
    </main>
    <div class="content">
        <div>
            Filter
            <select id="filter" class="form-select d-inline" style="max-width: 159px; min-width: 100px;">
               
            </select>
        </div>
        <div class="card card-body">
             
        </div>
    </div>
</section>